# 0003 - Dot-Separated Command Structure

`mxspots` exposes explicit dot-separated subcommands: `mxspots.findspots`, `mxspots.score`, and `mxspots.index`. This provides focused tools for each stage of quality analysis while matching established operational conventions.
